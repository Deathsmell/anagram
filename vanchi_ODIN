
/*
    Суть таcки(задания), написать программу(буковки и циферки) которая выясняет личные секреты пользователя.
    При запуске на главном экране должен вылазить вопрос "сколько раз в день ты жрешь скотина?"
    И окно ответа для пользователя
    Если ответ 3:
      То окно с ответом" Збс, четко!!!"
    Если меньше: 
      "Пидр дрищавый!!!"
    Если больше: 
      "Пидр жирный!!!"
    Усовершенствование фраз приветствуется
    Сделать через конструкцию if else и switch case(две разные программы)
    Земля металлом
*/



  switch (переменная) {
    case 'значение1':
      здесь код, который выполнится в случае, если переменная имеет значение1;
    break;
    case 'значение2':
      здесь код, который выполнится в случае, если переменная имеет значение2;
    break;
    case 'значение3':
      здесь код, который выполнится в случае, если переменная имеет значение3;
    break;
    default:
      этот код выполнится в случае, если переменная не совпала ни с одним значением;
    break;
  }


  if ( tag == 'значение1') {
    alert('вывод, если переменная имеет значение1');
  } 
  if (tag == 'значение2') { 
    alert('вывод, если переменная имеет значение2');
  }
  if (tag == 'значение2') { 
    alert('вывод, если переменная имеет значение3');
  }
